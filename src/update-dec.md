

Issues resolved:
1. PDM Dashboard not showing any data on changing location + no graph data.
    => resolved, backend response structure was changed, so data was not showing, its fixed now.

2. Alarms multiple page fetching, I have given a small forward arrow to fetch more and more pages for "Unaddressed" and "addressed alarms"

3. Infintie loading on Work Orders => This was not coming in debug, but came in production, its fixed now.

4. PDM Dashboard asset health status graph is now a donut graph.

5. Location details page UI modifications

6. Spectrum Graph Oritentation - Implemented Portrait to landscape and vice versa.

7. Asset Details graph - Spaced x axis timestamps, points will be clickable if flag is false in response.